<h1>BURGSSS!</h1>

<h2>I WANT A TASTY BOBS BURGER NOW!</h2>

<ul>
  {{#each burgs}}
    {{#unless devour}}
      {{> burgs/burgs-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>A Double cheese with bacon burger sounds nice!</h2>

<ul>
  {{#each burgs}}
    {{#if devour}}
      {{> burgs/burgs-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bb">Burger Name:</label>
    <input type="text" id="bb" name="name">
  </div>

  <div class="form-group">
    <label for="devour">devour it?</label><br>
    <input type="radio" name="devour" value="1" checked> cheesy!<br>
    <input type="radio" name="devour" value="0"> Bobs burger
  </div>

  <button type="submit">Add Burger</button>
</form>
